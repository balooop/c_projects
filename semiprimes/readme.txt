This is a program I wrote in its entirety for ECE220.
It has two components: the first checks if a number is prime, and is a helper function.
                     : the second prints all semiprimes between its two inputs